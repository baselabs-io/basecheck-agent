# Control-pack signing key

See `../docs/SIGNING.md` for what this directory is, why the key here is a
development key rather than a production one, and what to do before a real
production release. That file ships in release archives (via `docs/*.md`);
this directory does not, so it stays a pointer rather than the source of
truth.
